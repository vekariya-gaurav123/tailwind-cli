git init : for initilization
git status : for check git status
git add . for prepare your file for changes
git commit -m  for "commit text" you can commit them
git push origin -u branch name for your branch in github for push our code
git branch: check your branch list
git remote add origin https://github.com/USERNAME/REPO_NAME.git : It connects our local project (VS Code folder) to a GitHub repository
git branch  : check branch name